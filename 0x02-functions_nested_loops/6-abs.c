#include "main.h"
#include <stdio.h>

/**
  * _abs - Computes the absolute value.
  * of an integer
  *
  *
  * @r: input number as an integer
  *
  * Return: Absolute value.
  */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);

}
